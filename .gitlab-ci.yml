build:
  image: golang:1.16-buster
  script:
    - go mod download
    - go build -o build/app
  artifacts:
    paths:
      - build/

run:
  image: alpine:3
  needs:
    - job: build
      artifacts: true
  script:
    - ./build/app
