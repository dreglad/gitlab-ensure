stages:
  - build
  - run

build:
  stage: build
  image: golang:1.16-buster
  script:
    - go mod download
    - go build -o build/app
  artifacts:
    paths:
      - build/app

run:
  stage: run
  image: alpine:3
  script:
    - ./build/app
